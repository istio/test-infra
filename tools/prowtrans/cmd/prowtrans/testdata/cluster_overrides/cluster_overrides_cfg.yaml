transforms:

- mapping:
    istio: istio-private
  input: {{.Input}}
  output: {{.Output}}
  extra-refs:
  - org: istio
    repo: tools
    base_ref: master
    path_alias: istio.io/tools
  cluster_overrides:
    cluster_changeme: cluster_overridden_correctly
